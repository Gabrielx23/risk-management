import { Language } from '../../shared/enum/language.enum';
import { Role } from '../../shared/enum/role.enum';

export const DEFAULT_USER_ROLE = Role.notActive;
export const DEFAULT_USER_SETTINGS = {
  language: Language.pl,
};
